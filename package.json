{
  "name": "rxjs-better-if",
  "version": "0.0.1",
  "description": "A better if-else operator to rxjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "keywords": ["rxjs", "operator", "if", "else"]
}
